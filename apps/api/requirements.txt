# FastAPI & ASGI
fastapi==0.139.0
starlette==1.3.1  # pulled in by fastapi>=0.46.0 constraint; pinned explicitly after the 0.41.3 CVE batch
uvicorn[standard]==0.32.1
python-multipart==0.0.32

# Pydantic v2
pydantic==2.9.2
pydantic-settings==2.6.1

# SQLAlchemy 2.0 + async driver
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Authentication
email-validator==2.2.0
PyJWT==2.13.0  # replaced python-jose 3.3.0 (CVE-2024-33663 algorithm confusion, CVE-2024-33664 DoS)
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 misreads the version API on bcrypt>=4.1/5.x and raises spurious "72 bytes" errors
python-dotenv==1.2.2
cryptography==49.0.0

# Rate limiting
slowapi==0.1.9

# Email — Resend
resend==2.32.2

# Redis
redis==5.2.0
hiredis==3.0.0

# AWS S3
boto3==1.35.66
botocore==1.35.66

# AI / ML
# Groq is called via plain httpx REST — no SDK needed (avoids pydantic version conflicts)
scikit-learn==1.5.2
opencv-python-headless==4.10.0.84
numpy==1.26.4
pillow==12.3.0
tensorflow-cpu==2.18.0

# Pinecone vector store (optional — leave PINECONE_API_KEY blank to use PostgreSQL fallback)
# pinecone-client==5.0.1

# PostgreSQL vector extension (pgvector)
pgvector==0.3.5

# HTTP client
httpx==0.27.2
aiohttp==3.14.1

# HTML parsing (product image backfill script)
beautifulsoup4==4.15.0
lxml==6.1.1

# Utilities
python-slugify==8.0.4
pytz==2024.2
pyotp==2.9.0
qrcode==8.0

# Security — HTML sanitization, file type detection
nh3==0.2.18
python-magic==0.4.27  # cross-platform; CI installs libmagic1 via apt; Windows users need to install libmagic separately

# Security linting (used in CI — not imported at runtime)
bandit==1.7.10

# Monitoring
sentry-sdk[fastapi]==2.18.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
httpx==0.27.2
factory-boy==3.3.1
faker==30.8.2
